import { ChangeEvent, FormEvent, useEffect, useRef, useState } from 'react';
import { Bell, Camera, Loader2, Lock, Save, Settings as SettingsIcon, ShieldCheck, Volume2, X } from 'lucide-react';
import { supabase } from '../../lib/supabase';
import { useAuth } from '../../lib/auth';

type Profile = { id: string; email: string; full_name: string | null; avatar_url: string | null };
type NotificationPrefs = { user_id: string; message_notifications: boolean; call_notifications: boolean; sound_enabled: boolean; vibration_enabled: boolean; message_sound_url: string | null; voice_ringtone_url: string | null; video_ringtone_url: string | null };
type SoundField = 'message_sound_url' | 'voice_ringtone_url' | 'video_ringtone_url';

export default function Settings() {
  const { user } = useAuth(); const fileRef = useRef<HTMLInputElement | null>(null);
  const [profile, setProfile] = useState<Profile | null>(null); const [name, setName] = useState(''); const [avatarUrl, setAvatarUrl] = useState(''); const [editing, setEditing] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [uploading, setUploading] = useState(false); const [soundUploading, setSoundUploading] = useState<SoundField | null>(null); const [message, setMessage] = useState<string | null>(null); const [error, setError] = useState<string | null>(null); const [prefs, setPrefs] = useState<NotificationPrefs | null>(null); const [savingPrefs, setSavingPrefs] = useState(false); const [currentPassword, setCurrentPassword] = useState(''); const [newPassword, setNewPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [passwordBusy, setPasswordBusy] = useState(false); const [passwordMessage, setPasswordMessage] = useState<string | null>(null); const [passwordError, setPasswordError] = useState<string | null>(null);

  useEffect(() => { if (!user?.id) { setLoading(false); return; } let mounted = true; const load = async () => { setLoading(true); const [{ data: profileRow, error: profileError }, { data: prefRow }] = await Promise.all([supabase.from('profiles').select('id,email,full_name,avatar_url').eq('id', user.id).maybeSingle(), supabase.from('user_notification_preferences').select('*').eq('user_id', user.id).maybeSingle()]); if (!mounted) return; if (profileError) setError('We could not load your profile right now.'); if (profileRow) { const p = profileRow as Profile; setProfile(p); setName(p.full_name || ''); setAvatarUrl(p.avatar_url || ''); } setPrefs((prefRow as NotificationPrefs | null) || { user_id: user.id, message_notifications: true, call_notifications: true, sound_enabled: true, vibration_enabled: true, message_sound_url: null, voice_ringtone_url: null, video_ringtone_url: null }); setLoading(false); }; void load(); return () => { mounted = false; }; }, [user?.id]);

  const uploadAvatar = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file || !user?.id) return; if (!['image/jpeg','image/png','image/webp','image/gif'].includes(file.type)) { setError('Please choose a JPG, PNG, WebP, or GIF image.'); return; } if (file.size > 5 * 1024 * 1024) { setError('Profile pictures must be 5 MB or smaller.'); return; } setUploading(true); setError(null); try { const extension = file.name.split('.').pop()?.toLowerCase() || 'jpg'; const path = `${user.id}/${crypto.randomUUID()}.${extension}`; const { error: uploadError } = await supabase.storage.from('profile-avatars').upload(path, file, { upsert: true, contentType: file.type, cacheControl: '3600' }); if (uploadError) throw uploadError; const { data } = supabase.storage.from('profile-avatars').getPublicUrl(path); const { error: profileError } = await supabase.from('profiles').upsert({ id: user.id, email: user.email || profile?.email || '', full_name: name.trim() || null, avatar_url: data.publicUrl }, { onConflict: 'id' }); if (profileError) throw profileError; setAvatarUrl(data.publicUrl); setProfile((current) => current ? { ...current, avatar_url: data.publicUrl } : { id: user.id, email: user.email || '', full_name: name.trim() || null, avatar_url: data.publicUrl }); setMessage('Profile picture updated successfully.'); } catch (uploadError) { console.error(uploadError); setError('Your profile picture could not be uploaded. Please try again.'); } finally { setUploading(false); if (fileRef.current) fileRef.current.value = ''; } };

  const uploadSound = async (field: SoundField, file: File) => { if (!user?.id) return; const allowed = ['audio/mpeg','audio/wav','audio/ogg','audio/webm','audio/mp4']; if (!allowed.includes(file.type)) { setError('Please choose an MP3, WAV, OGG, WebM, or MP4 audio file.'); return; } if (file.size > 3 * 1024 * 1024) { setError('Custom sounds must be 3 MB or smaller.'); return; } setSoundUploading(field); setError(null); setMessage(null); try { const extension = file.name.split('.').pop()?.toLowerCase() || 'mp3'; const path = `${user.id}/${field}-${crypto.randomUUID()}.${extension}`; const { error: uploadError } = await supabase.storage.from('notification-sounds').upload(path, file, { upsert: true, contentType: file.type, cacheControl: '3600' }); if (uploadError) throw uploadError; const { data } = supabase.storage.from('notification-sounds').getPublicUrl(path); setPrefs((current) => current ? { ...current, [field]: data.publicUrl } : current); setMessage(`${field === 'message_sound_url' ? 'Message sound' : field === 'voice_ringtone_url' ? 'Voice ringtone' : 'Video ringtone'} updated.`); } catch (uploadError) { console.error(uploadError); setError('The custom sound could not be uploaded.'); } finally { setSoundUploading(null); } };

  const saveProfile = async (event: FormEvent) => { event.preventDefault(); if (!user?.id) return; setSaving(true); setError(null); setMessage(null); try { const { data, error: saveError } = await supabase.from('profiles').upsert({ id: user.id, email: user.email || profile?.email || '', full_name: name.trim() || null, avatar_url: avatarUrl.trim() || null }, { onConflict: 'id' }).select('id,email,full_name,avatar_url').single(); if (saveError) throw saveError; setProfile(data as Profile); setEditing(false); setMessage('Your profile information has been updated.'); } catch (saveError) { console.error(saveError); setError('Your profile could not be saved right now.'); } finally { setSaving(false); } };
  const savePreferences = async () => { if (!user?.id || !prefs) return; setSavingPrefs(true); setError(null); setMessage(null); const { error: prefError } = await supabase.from('user_notification_preferences').upsert({ ...prefs, user_id: user.id, updated_at: new Date().toISOString() }, { onConflict: 'user_id' }); if (prefError) setError('Notification preferences could not be saved.'); else setMessage('Notification preferences saved.'); setSavingPrefs(false); };
  const changePassword = async (event: FormEvent) => { event.preventDefault(); setPasswordMessage(null); setPasswordError(null); if (!user?.email) { setPasswordError('Your account email could not be determined.'); return; } if (!currentPassword) { setPasswordError('Please enter your current password.'); return; } if (newPassword.length < 8) { setPasswordError('Your new password must be at least 8 characters long.'); return; } if (newPassword !== confirmPassword) { setPasswordError('The new passwords do not match.'); return; } setPasswordBusy(true); try { const { error: verifyError } = await supabase.auth.signInWithPassword({ email: user.email, password: currentPassword }); if (verifyError) { setPasswordError('Your current password is incorrect.'); return; } const { error: updateError } = await supabase.auth.updateUser({ password: newPassword }); if (updateError) throw updateError; setCurrentPassword(''); setNewPassword(''); setConfirmPassword(''); setPasswordMessage('Your password has been changed successfully.'); } catch (passwordChangeError) { setPasswordError(passwordChangeError instanceof Error ? passwordChangeError.message : 'Your password could not be changed right now.'); } finally { setPasswordBusy(false); } };

  if (loading) return <div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-accent-400" /></div>;
  const initials = (name || user?.email || 'U').trim().charAt(0).toUpperCase();
  const soundRows: Array<{ field: SoundField; label: string; accept: string }> = [{ field: 'message_sound_url', label: 'Message sound', accept: 'audio/*' }, { field: 'voice_ringtone_url', label: 'Voice-call ringtone', accept: 'audio/*' }, { field: 'video_ringtone_url', label: 'Video-call ringtone', accept: 'audio/*' }];
  return <div className="space-y-6"><div><h1 className="text-3xl font-semibold text-white">Settings</h1><p className="mt-2 text-gray-400">Manage your Avelixa profile, notifications, sounds and account security.</p></div>{(message || error) && <div className={`rounded-xl border px-4 py-3 text-sm ${error ? 'border-red-500/20 bg-red-500/10 text-red-300' : 'border-emerald-500/20 bg-emerald-500/10 text-emerald-300'}`}>{error || message}</div>}
    <div className="grid gap-6 xl:grid-cols-[1.05fr_0.95fr]"><div className="space-y-6"><section className="glass rounded-2xl border border-ink-800/50 p-6 sm:p-8"><div className="flex items-center gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-accent-500/10"><SettingsIcon className="h-6 w-6 text-accent-400" /></div><div><h2 className="text-xl font-medium text-white">Profile</h2><p className="text-sm text-gray-400">Your name and profile picture are used across Avelixa.</p></div></div><div className="mt-6 flex flex-col gap-5 sm:flex-row sm:items-center"><div className="relative h-24 w-24 shrink-0"><div className="h-24 w-24 overflow-hidden rounded-full border border-white/10 bg-accent-500/10 flex items-center justify-center text-2xl font-semibold text-accent-300">{avatarUrl ? <img src={avatarUrl} alt="Profile" className="h-full w-full object-cover" /> : initials}</div><button type="button" onClick={() => fileRef.current?.click()} disabled={uploading} className="absolute -bottom-1 -right-1 flex h-9 w-9 items-center justify-center rounded-full border-2 border-ink-950 bg-accent-500 text-white shadow-lg hover:bg-accent-400 disabled:opacity-60">{uploading ? <Loader2 className="h-4 w-4 animate-spin" /> : <Camera className="h-4 w-4" />}</button><input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={uploadAvatar} className="hidden" /></div><div><div className="text-sm font-semibold text-white">{name || 'No display name set'}</div><div className="mt-1 text-sm text-gray-400">{user?.email}</div><button type="button" onClick={() => fileRef.current?.click()} disabled={uploading} className="mt-3 inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-xs font-semibold text-gray-300 hover:text-white disabled:opacity-60"><Camera className="h-4 w-4" />{uploading ? 'Uploading…' : 'Change picture'}</button></div></div><form onSubmit={saveProfile} className="mt-7 space-y-5"><label className="block text-sm"><span className="mb-2 block text-gray-300">Full name</span><input value={name} onChange={(event) => setName(event.target.value)} disabled={!editing || saving} className="w-full rounded-xl border border-ink-800/60 bg-white/5 px-4 py-3 text-sm text-white outline-none focus:border-accent-500 disabled:opacity-70" placeholder="Enter your full name" /></label><div className="flex gap-3">{!editing ? <button type="button" onClick={() => setEditing(true)} className="rounded-xl bg-accent-600 px-4 py-2.5 text-sm font-medium text-white hover:bg-accent-500">Edit profile</button> : <><button type="submit" disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-accent-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-60">{saving && <Loader2 className="h-4 w-4 animate-spin" />}<Save className="h-4 w-4" />Save changes</button><button type="button" onClick={() => { setName(profile?.full_name || ''); setEditing(false); }} className="inline-flex items-center gap-2 rounded-xl border border-white/10 px-4 py-2.5 text-sm text-gray-300 hover:text-white"><X className="h-4 w-4" />Cancel</button></>}</div></form></section>
    <section className="glass rounded-2xl border border-ink-800/50 p-6"><div className="flex items-center gap-3"><ShieldCheck className="h-5 w-5 text-accent-400" /><div><h3 className="text-lg font-medium text-white">Password & Security</h3><p className="text-sm text-gray-400">Change your Avelixa password.</p></div></div>{passwordMessage && <div className="mt-4 rounded-xl bg-emerald-500/10 px-4 py-3 text-sm text-emerald-300">{passwordMessage}</div>}{passwordError && <div className="mt-4 rounded-xl bg-red-500/10 px-4 py-3 text-sm text-red-300">{passwordError}</div>}<form onSubmit={changePassword} className="mt-5 space-y-4"><input type="password" value={currentPassword} onChange={(e) => setCurrentPassword(e.target.value)} placeholder="Current password" className="w-full rounded-xl border border-ink-800/60 bg-white/5 px-4 py-3 text-sm text-white outline-none focus:border-accent-500" required /><input type="password" value={newPassword} onChange={(e) => setNewPassword(e.target.value)} placeholder="New password" className="w-full rounded-xl border border-ink-800/60 bg-white/5 px-4 py-3 text-sm text-white outline-none focus:border-accent-500" required /><input type="password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder="Confirm new password" className="w-full rounded-xl border border-ink-800/60 bg-white/5 px-4 py-3 text-sm text-white outline-none focus:border-accent-500" required /><button type="submit" disabled={passwordBusy} className="inline-flex items-center gap-2 rounded-xl bg-accent-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-60">{passwordBusy && <Loader2 className="h-4 w-4 animate-spin" />}Change password</button></form></section></div>
    <section className="glass rounded-2xl border border-ink-800/50 p-6 h-fit"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-accent-500/10"><Bell className="h-5 w-5 text-accent-400" /></div><div><h3 className="text-lg font-medium text-white">Notifications & Sounds</h3><p className="text-sm text-gray-400">Control how Avelixa alerts you.</p></div></div>{prefs && <div className="mt-6 space-y-5"><label className="flex items-center justify-between gap-4"><span><span className="block text-sm font-medium text-white">Message notifications</span><span className="text-xs text-gray-500">Show new-message alerts and unread badges.</span></span><input type="checkbox" checked={prefs.message_notifications} onChange={(e) => setPrefs({ ...prefs, message_notifications: e.target.checked })} /></label><label className="flex items-center justify-between gap-4"><span><span className="block text-sm font-medium text-white">Call notifications</span><span className="text-xs text-gray-500">Allow incoming voice/video call alerts.</span></span><input type="checkbox" checked={prefs.call_notifications} onChange={(e) => setPrefs({ ...prefs, call_notifications: e.target.checked })} /></label><label className="flex items-center justify-between gap-4"><span className="flex items-center gap-2 text-sm font-medium text-white"><Volume2 className="h-4 w-4 text-accent-400" />Notification sounds</span><input type="checkbox" checked={prefs.sound_enabled} onChange={(e) => setPrefs({ ...prefs, sound_enabled: e.target.checked })} /></label><label className="flex items-center justify-between gap-4"><span><span className="block text-sm font-medium text-white">Vibration</span><span className="text-xs text-gray-500">Use vibration where supported.</span></span><input type="checkbox" checked={prefs.vibration_enabled} onChange={(e) => setPrefs({ ...prefs, vibration_enabled: e.target.checked })} /></label><div className="rounded-2xl border border-white/10 bg-white/[0.03] p-4 space-y-3"><div className="text-sm font-medium text-white">Custom sounds</div>{soundRows.map((row) => <div key={row.field} className="flex items-center justify-between gap-3 rounded-xl border border-white/5 bg-white/[0.02] p-3"><div className="min-w-0"><div className="text-xs font-medium text-gray-200">{row.label}</div>{prefs[row.field] ? <audio controls src={prefs[row.field] || undefined} className="mt-2 h-8 max-w-full" /> : <div className="mt-1 text-[11px] text-gray-600">Default sound</div>}</div><label className="shrink-0 cursor-pointer rounded-lg border border-white/10 bg-white/5 px-3 py-2 text-[11px] font-semibold text-gray-300 hover:text-white">{soundUploading === row.field ? 'Uploading…' : 'Choose audio'}<input type="file" accept={row.accept} disabled={soundUploading !== null} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadSound(row.field, file); e.currentTarget.value = ''; }} className="hidden" /></label></div>)}</div><button type="button" onClick={() => void savePreferences()} disabled={savingPrefs} className="inline-flex items-center gap-2 rounded-xl bg-accent-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-60">{savingPrefs && <Loader2 className="h-4 w-4 animate-spin" />}<Save className="h-4 w-4" />Save notification settings</button></div>}</section></div></div>;
}
