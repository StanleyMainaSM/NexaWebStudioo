import { FormEvent, useEffect, useMemo, useRef, useState, lazy, Suspense } from 'react';
import { ArrowLeft, Ban, BellOff, Check, CheckCheck, Download, Loader2, MessageCirclePlus, MessageSquare, MoreVertical, Phone, Plus, Search, Send, UserPlus, Video, Volume2, X } from 'lucide-react';
import { useAuth } from '../../lib/auth';
import { supabase } from '../../lib/supabase';
import type { ActiveCall } from '../../components/portal/CallOverlayV2';
const CallOverlay=lazy(()=>import('../../components/portal/CallOverlayV2'));
type Person={user_id:string;full_name:string|null;email:string|null;is_online:boolean;last_seen_at:string|null;avatar_url?:string|null;contact_name?:string|null;role_context?:string|null};
type Conversation={id:string;otherUserId:string;otherName:string;otherEmail:string|null;otherRole:string|null;updated_at:string;kind:'direct'|'admin'};
type Message={id:string;conversation_id:string;sender_id:string;content:string;read_at:string|null;created_at:string;kind:'direct'|'admin'};
type CallEvent={id:string;call_type:'voice'|'video';status:string;caller_id:string;callee_id:string;created_at:string;duration_seconds:number|null;conversation_id:string};
const nameOf=(n?:string|null,e?:string|null,c?:string|null)=>c?.trim()||n?.trim()||e?.trim()||'Avelixa User';
const errOf=(e:unknown)=>{if(e&&typeof e==='object'){const x=e as any;return [x.message,x.details,x.hint&&`Hint: ${x.hint}`,x.code&&`Code: ${x.code}`].filter(Boolean).join(' • ')||'Something went wrong.'}return String(e||'Something went wrong.')};
const timeOf=(v:string)=>new Date(v).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});
const dateOf=(v:string)=>new Date(v).toLocaleString([],{dateStyle:'medium',timeStyle:'short'});
function Avatar({p}:{p?:Person|null}){const label=nameOf(p?.full_name,p?.email,p?.contact_name);return <div className="relative h-11 w-11 shrink-0 overflow-hidden rounded-full border border-white/10 bg-accent-500/10 flex items-center justify-center font-semibold text-accent-300">{p?.avatar_url?<img src={p.avatar_url} className="h-full w-full object-cover" alt=""/>:label[0]?.toUpperCase()}<span className={`absolute bottom-0 right-0 h-3 w-3 rounded-full border-2 border-ink-950 ${p?.is_online?'bg-emerald-400':'bg-gray-500'}`}/></div>}
export default function CommunicationCenterV3(){
 const {user,roles}=useAuth(); const management=roles.some(r=>['owner','admin'].includes(String(r).toLowerCase()));
 const [conversations,setConversations]=useState<Conversation[]>([]),[contacts,setContacts]=useState<Person[]>([]),[selectedId,setSelectedId]=useState<string|null>(null),[messages,setMessages]=useState<Message[]>([]),[calls,setCalls]=useState<CallEvent[]>([]),[unread,setUnread]=useState<Record<string,number>>({});
 const [people,setPeople]=useState<Person[]>([]),[query,setQuery]=useState(''),[chatSearch,setChatSearch]=useState(''),[newOpen,setNewOpen]=useState(false),[contactName,setContactName]=useState(''),[email,setEmail]=useState(''),[found,setFound]=useState<Person|null>(null),[lookupBusy,setLookupBusy]=useState(false),[notice,setNotice]=useState(''),[error,setError]=useState(''),[text,setText]=useState(''),[menu,setMenu]=useState(false),[blocked,setBlocked]=useState(false),[muted,setMuted]=useState(false),[wallpaper,setWallpaper]=useState('default'),[activeCall,setActiveCall]=useState<ActiveCall|null>(null),[incoming,setIncoming]=useState<CallEvent|null>(null),[loading,setLoading]=useState(true),[sending,setSending]=useState(false);
 const listRef=useRef<HTMLDivElement>(null),threadRef=useRef<HTMLDivElement>(null),nearBottom=useRef(true),firstThread=useRef(true); const selected=conversations.find(c=>c.id===selectedId)||null; const selectedPerson=selected?contacts.find(c=>c.user_id===selected.otherUserId)||null:null;
 const refreshContacts=async()=>{if(!user)return;const {data:rows,error:e}=await supabase.from('user_contacts').select('contact_user_id,contact_name').eq('user_id',user.id);if(e)throw e;const ids=(rows||[]).map(r=>r.contact_user_id);if(!ids.length){setContacts([]);return}const [{data:ps},{data:pr}]=await Promise.all([supabase.from('profiles').select('id,full_name,email,avatar_url').in('id',ids),supabase.from('user_presence').select('user_id,is_online,last_seen_at').in('user_id',ids)]);const pm=new Map((ps||[]).map(p=>[p.id,p])),sm=new Map((pr||[]).map(p=>[p.user_id,p]));setContacts((rows||[]).map(r=>{const p=pm.get(r.contact_user_id),s=sm.get(r.contact_user_id);return {user_id:r.contact_user_id,full_name:p?.full_name||null,email:p?.email||null,avatar_url:p?.avatar_url||null,contact_name:r.contact_name||null,is_online:!!s?.is_online,last_seen_at:s?.last_seen_at||null}}))};
 const refreshUnread=async(cs:Conversation[])=>{if(!user)return;const counts:Record<string,number>={};const ds=cs.filter(c=>c.kind==='direct').map(c=>c.id);if(ds.length){const {data}=await supabase.from('direct_messages').select('conversation_id').in('conversation_id',ds).neq('sender_id',user.id).is('read_at',null);(data||[]).forEach((r:any)=>counts[r.conversation_id]=(counts[r.conversation_id]||0)+1)}setUnread(counts)};
 const loadConversations=async()=>{if(!user)return;const {data,error:e}=await supabase.rpc('list_direct_conversations');if(e)throw e;const direct=((data||[]) as any[]).map(r=>({kind:'direct' as const,id:r.conversation_id,otherUserId:r.other_user_id,otherName:nameOf(r.other_full_name,r.other_email),otherEmail:r.other_email,otherRole:r.other_role,updated_at:r.updated_at}));let admin:Conversation[]=[];if(!management){const {data:cid}=await supabase.rpc('get_or_create_admin_portal_conversation');if(cid){const {data:r}=await supabase.from('admin_conversations').select('id,user_id,admin_id,updated_at').eq('id',cid).maybeSingle();if(r)admin=[{kind:'admin',id:r.id,otherUserId:r.admin_id,otherName:'Avelixa Admin',otherEmail:null,otherRole:'admin',updated_at:r.updated_at}]}}const cs=[...direct,...admin].sort((a,b)=>+new Date(b.updated_at)-+new Date(a.updated_at));setConversations(cs);await refreshUnread(cs)};
 useEffect(()=>{if(!user)return;let alive=true;(async()=>{try{await supabase.rpc('communication_set_presence',{p_online:true});await Promise.all([loadConversations(),refreshContacts()])}catch(e){if(alive)setError(errOf(e))}finally{if(alive)setLoading(false)}})();const beat=window.setInterval(()=>void supabase.rpc('communication_set_presence',{p_online:true}),25000);const off=()=>{void supabase.rpc('communication_set_presence',{p_online:false})};window.addEventListener('beforeunload',off);return()=>{alive=false;window.clearInterval(beat);window.removeEventListener('beforeunload',off);off()}},[user?.id,management]);
 useEffect(()=>{if(!user)return;const ch=supabase.channel(`communication-v3-${user.id}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_messages'},({new:m}:any)=>{if(m.sender_id===user.id)return;if(m.conversation_id===selectedId){setMessages(v=>v.some(x=>x.id===m.id)?v:[...v,{...m,kind:'direct'}]);void markReadId(m.conversation_id)}else setUnread(v=>({...v,[m.conversation_id]:(v[m.conversation_id]||0)+1}))}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'direct_messages'},({new:m}:any)=>setMessages(v=>v.map(x=>x.id===m.id?{...x,...m}:x))).on('postgres_changes',{event:'INSERT',schema:'public',table:'call_sessions'},({new:c}:any)=>{const cid=c.direct_conversation_id||c.admin_conversation_id;if(cid===selectedId)setCalls(v=>v.some(x=>x.id===c.id)?v:[...v,{...c,conversation_id:cid}]);if(c.callee_id===user.id&&c.status==='ringing')setIncoming({...c,conversation_id:cid})}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'call_sessions'},({new:c}:any)=>{if(c.direct_conversation_id===selectedId||c.admin_conversation_id===selectedId)setCalls(v=>v.map(x=>x.id===c.id?{...x,...c}:x));if(incoming?.id===c.id&&c.status!=='ringing')setIncoming(null)}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'user_presence'},({new:p}:any)=>setContacts(v=>v.map(x=>x.user_id===p.user_id?{...x,is_online:!!p.is_online,last_seen_at:p.last_seen_at||null}:x))).subscribe();return()=>{void supabase.removeChannel(ch)}},[user?.id,selectedId,incoming?.id]);
 const markReadId=async(id:string)=>{if(!user)return;const {data}=await supabase.from('direct_messages').select('id').eq('conversation_id',id).neq('sender_id',user.id).is('read_at',null);const ids=(data||[]).map((r:any)=>r.id);if(!ids.length){setUnread(v=>({...v,[id]:0}));return}const now=new Date().toISOString();const {error:e}=await supabase.from('direct_messages').update({read_at:now}).in('id',ids);if(!e){setMessages(v=>v.map(m=>ids.includes(m.id)?{...m,read_at:now}:m));setUnread(v=>({...v,[id]:0}))}};
 useEffect(()=>{if(!selected||!user)return;let alive=true;firstThread.current=true;(async()=>{try{const [{data:m,error:me},{data:c,error:ce},{data:p},{data:b}]=await Promise.all([selected.kind==='direct'?supabase.from('direct_messages').select('*').eq('conversation_id',selected.id).order('created_at',{ascending:true}):supabase.from('admin_messages').select('*').eq('conversation_id',selected.id).order('created_at',{ascending:true}),supabase.from('call_sessions').select('id,call_type,status,caller_id,callee_id,created_at,duration_seconds').or(`direct_conversation_id.eq.${selected.id},admin_conversation_id.eq.${selected.id}`).order('created_at',{ascending:true}),selected.kind==='direct'?supabase.from('conversation_preferences').select('*').eq('user_id',user.id).eq('conversation_id',selected.id).maybeSingle():Promise.resolve({data:null} as any),selected.kind==='direct'?supabase.from('user_blocks').select('id').eq('blocker_id',user.id).eq('blocked_id',selected.otherUserId).maybeSingle():Promise.resolve({data:null} as any)]);if(me||ce)throw me||ce;if(!alive)return;setMessages(((m||[]) as any[]).map(x=>({...x,kind:selected.kind})));setCalls(((c||[]) as any[]).map(x=>({...x,conversation_id:selected.id})));setMuted(!!p?.muted);setWallpaper(p?.wallpaper||'default');setBlocked(!!b);await markReadId(selected.id)}catch(e){if(alive)setError(errOf(e))}})();return()=>{alive=false}},[selectedId,user?.id]);
 const timeline=useMemo(()=>[...messages.filter(m=>!chatSearch||m.content.toLowerCase().includes(chatSearch.toLowerCase())).map(m=>({kind:'message' as const,at:m.created_at,item:m})),...calls.map(c=>({kind:'call' as const,at:c.created_at,item:c}))].sort((a,b)=>+new Date(a.at)-+new Date(b.at)),[messages,calls,chatSearch]);
 useEffect(()=>{const el=threadRef.current;if(!el)return;const onScroll=()=>{nearBottom.current=el.scrollHeight-el.scrollTop-el.clientHeight<160};el.addEventListener('scroll',onScroll,{passive:true});if(firstThread.current){requestAnimationFrame(()=>{el.scrollTop=el.scrollHeight;firstThread.current=false;nearBottom.current=true})}return()=>el.removeEventListener('scroll',onScroll)},[selectedId,timeline.length]);
 useEffect(()=>{if(nearBottom.current&&threadRef.current&&!firstThread.current)requestAnimationFrame(()=>{if(threadRef.current)threadRef.current.scrollTop=threadRef.current.scrollHeight})},[timeline.length]);
 const searchPerson=async(e:FormEvent)=>{e.preventDefault();if(!email.trim())return;setLookupBusy(true);setError('');try{const {data,error:e}=await supabase.rpc('find_communication_user',{p_identifier:email.trim()});if(e)throw e;setFound(((data||[]) as Person[])[0]||null)}catch(e){setError(errOf(e))}finally{setLookupBusy(false)}};
 const saveOrInvite=async()=>{if(!user||!contactName.trim()||!email.trim()){setError('Enter the person's name and email.');return}setError('');try{if(found){await supabase.rpc('communication_add_contact',{p_contact_user_id:found.user_id,p_contact_name:contactName.trim()});const {data:cid,error:e}=await supabase.rpc('get_or_create_direct_conversation',{p_recipient_id:found.user_id});if(e)throw e;await refreshContacts();await loadConversations();setSelectedId(cid as string);setNewOpen(false);setContactName('');setEmail('');setFound(null);setNotice('Contact saved.')}else{const {error:e}=await supabase.functions.invoke('avelixa-invite-chat-user',{body:{email:email.trim(),full_name:contactName.trim(),redirect_to:window.location.origin}});if(e)throw e;setNotice(`Invitation sent to ${email.trim()}.`);setNewOpen(false);setContactName('');setEmail('');setFound(null)}}catch(e){setError(errOf(e))}};
 const sendMessage=async(e:FormEvent)=>{e.preventDefault();if(!user||!selected||blocked||!text.trim()||sending)return;setSending(true);try{const table=selected.kind==='direct'?'direct_messages':'admin_messages';const payload=selected.kind==='direct'?{conversation_id:selected.id,sender_id:user.id,content:text.trim()}:{conversation_id:selected.id,sender_id:user.id,recipient_id:selected.otherUserId,content:text.trim()};const {data,error:e}=await supabase.from(table).insert(payload).select('*').single();if(e)throw e;if(data)setMessages(v=>v.some(m=>m.id===data.id)?v:[...v,{...data,kind:selected.kind}]);setText('');requestAnimationFrame(()=>{if(threadRef.current)threadRef.current.scrollTop=threadRef.current.scrollHeight});void loadConversations()}catch(e){setError(errOf(e))}finally{setSending(false)}};
 const startCall=async(type:'voice'|'video')=>{if(!user||!selected||blocked)return;try{const payload:any={caller_id:user.id,callee_id:selected.otherUserId,call_type:type,status:'ringing'};if(selected.kind==='direct')payload.direct_conversation_id=selected.id;else payload.admin_conversation_id=selected.id;const {data,error:e}=await supabase.from('call_sessions').insert(payload).select('*').single();if(e)throw e;setActiveCall({id:data.id,callType:type,callerId:user.id,calleeId:selected.otherUserId,remoteName:displaySelectedName,isIncoming:false,directConversationId:selected.kind==='direct'?selected.id:null,adminConversationId:selected.kind==='admin'?selected.id:null})}catch(e){setError(`Could not start call: ${errOf(e)}`)}};
 const displaySelectedName=selected?nameOf(selected.otherName,selected.otherEmail,selectedPerson?.contact_name):''; const chats=conversations.filter(c=>{const q=query.toLowerCase();const p=contacts.find(x=>x.user_id===c.otherUserId);return !q||nameOf(c.otherName,c.otherEmail,p?.contact_name).toLowerCase().includes(q)});const unreadTotal=Object.values(unread).reduce((a,b)=>a+b,0);
 if(!user)return <div className="p-6 text-red-300">Please sign in.</div>; if(loading)return <div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-accent-400"/></div>;
 return <div className="min-h-[calc(100vh-7rem)] overflow-hidden rounded-3xl border border-white/10 bg-ink-900/80"><div className="flex h-[calc(100vh-7rem)] min-h-[620px]"><aside className={`${selectedId?'hidden md:flex':'flex'} w-full md:w-[360px] shrink-0 flex-col border-r border-white/10 bg-ink-950`}><div className="border-b border-white/10 p-4"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-white">Messages {unreadTotal>0&&<span className="ml-2 rounded-full bg-red-500 px-2 py-0.5 text-xs">{unreadTotal}</span>}</h2><p className="text-xs text-gray-500">Saved contacts only</p></div><button onClick={()=>setNewOpen(true)} className="rounded-xl border border-white/10 p-2 text-gray-300"><MessageCirclePlus className="h-5 w-5"/></button></div><div className="mt-3 flex items-center gap-2"><Search className="h-4 w-4 text-gray-500"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search saved contacts" className="w-full bg-transparent py-2 text-sm text-white outline-none"/></div></div><div ref={listRef} className="flex-1 overflow-y-auto">{chats.map(c=>{const p=contacts.find(x=>x.user_id===c.otherUserId);const n=unread[c.id]||0;return <button key={c.id} onClick={()=>{setSelectedId(c.id);setMenu(false)}} className={`flex w-full items-center gap-3 border-b border-white/5 p-4 text-left ${selectedId===c.id?'bg-accent-500/10':''}`}><Avatar p={p}/><div className="min-w-0 flex-1"><div className="flex justify-between"><span className="truncate text-sm font-semibold text-white">{nameOf(c.otherName,c.otherEmail,p?.contact_name)}</span>{n>0&&<span className="rounded-full bg-red-500 px-2 py-0.5 text-[10px] text-white">{n}</span>}</div><div className={`text-xs ${p?.is_online?'text-emerald-400':'text-gray-500'}`}>{p?.is_online?'Online':'Offline'}</div></div></button>})}</div></aside><section className={`${selectedId?'flex':'hidden md:flex'} min-w-0 flex-1 flex-col relative ${wallpaper==='default'?'bg-ink-950':wallpaper==='midnight'?'bg-gradient-to-br from-indigo-950 via-slate-950 to-black':wallpaper==='forest'?'bg-gradient-to-br from-emerald-950 via-slate-950 to-black':'bg-gradient-to-br from-slate-950 via-slate-900 to-zinc-950'}`}><div className="pointer-events-none absolute inset-0 bg-black/45"/>{!selected?<div className="relative m-auto text-center text-gray-500">Select a conversation</div>:<><header className="relative flex items-center gap-3 border-b border-white/10 bg-ink-950/80 px-4 py-3"><button onClick={()=>setSelectedId(null)} className="md:hidden"><ArrowLeft/></button><Avatar p={selectedPerson}/><div className="flex-1"><div className="font-semibold text-white">{displaySelectedName}</div><div className={`text-xs ${selectedPerson?.is_online?'text-emerald-400':'text-gray-500'}`}>{selectedPerson?.is_online?'Online':'Offline'}</div></div><button onClick={()=>void startCall('voice')}><Phone className="h-5 w-5"/></button><button onClick={()=>void startCall('video')}><Video className="h-5 w-5"/></button><button onClick={()=>setMenu(v=>!v)}><MoreVertical/></button></header>{menu&&<div className="absolute right-3 top-16 z-30 w-52 rounded-xl border border-white/10 bg-ink-900 p-2 shadow-xl"><button onClick={()=>setMenu(false)} className="flex w-full gap-2 p-2 text-sm"><Search className="h-4 w-4"/>Search conversation</button><button onClick={exportChat} className="flex w-full gap-2 p-2 text-sm"><Download className="h-4 w-4"/>Export chat</button><button onClick={()=>void clearChat()} className="flex w-full gap-2 p-2 text-sm"><X className="h-4 w-4"/>Clear chat</button><button onClick={()=>void toggleMute()} className="flex w-full gap-2 p-2 text-sm"><BellOff className="h-4 w-4"/>{muted?'Unmute':'Mute'}</button><button onClick={()=>void toggleBlock()} className="flex w-full gap-2 p-2 text-sm text-red-300"><Ban className="h-4 w-4"/>{blocked?'Unblock':'Block'}</button><label className="flex w-full cursor-pointer gap-2 p-2 text-sm"><Volume2 className="h-4 w-4"/>Wallpaper<input type="file" accept="image/*" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setWallpaperValue(String(r.result));r.readAsDataURL(f)}}/></label></div>}<div className="relative flex items-center gap-2 border-b border-white/5 bg-ink-950/50 px-4 py-2"><Search className="h-4 w-4 text-gray-500"/><input value={chatSearch} onChange={e=>setChatSearch(e.target.value)} placeholder="Search this conversation" className="flex-1 bg-transparent text-xs text-white outline-none"/></div><div ref={threadRef} className="relative flex-1 overflow-y-auto p-4">{timeline.map(x=>x.kind==='message'?<div key={x.item.id} className={`mb-2 flex ${x.item.sender_id===user.id?'justify-end':'justify-start'}`}><div className={`max-w-[75%] rounded-2xl px-4 py-2 text-sm shadow ${x.item.sender_id===user.id?'bg-accent-600 text-white':'bg-ink-900/90 text-white'}`}><div>{x.item.content}</div><div className="mt-1 flex items-center justify-end gap-1 text-[10px] opacity-70">{timeOf(x.at)}{x.item.sender_id===user.id&&(x.item.read_at?<CheckCheck className="h-3.5 w-3.5 text-sky-300"/>:selectedPerson?.is_online?<CheckCheck className="h-3.5 w-3.5"/>:<Check className="h-3.5 w-3.5"/>)}</div></div></div>:<div key={x.item.id} className="mb-2 flex justify-center"><div className="rounded-full bg-ink-900/90 px-4 py-2 text-xs text-gray-300">{x.item.call_type==='video'?'Video':'Voice'} call · {x.item.status} · {dateOf(x.at)}</div></div>)}<div className="h-2"/></div><form onSubmit={sendMessage} className="relative flex gap-2 border-t border-white/10 bg-ink-950/80 p-3"><input disabled={blocked} value={text} onChange={e=>setText(e.target.value)} placeholder={blocked?'You blocked this user':'Message'} className="flex-1 rounded-xl bg-white/5 px-4 py-3 text-sm text-white outline-none"/><button disabled={sending||blocked} className="rounded-xl bg-accent-600 px-4"><Send className="h-5 w-5"/></button></form></>}</section></div>{newOpen&&<div className="fixed inset-0 z-[70] flex items-center justify-center bg-black/70 p-4"><div className="w-full max-w-md rounded-2xl border border-white/10 bg-ink-900 p-6"><div className="flex items-center justify-between"><h3 className="font-semibold text-white">Add contact</h3><button onClick={()=>setNewOpen(false)}><X/></button></div><p className="mt-2 text-xs text-gray-500">Avelixa will not show a public user directory. Add someone by name and email.</p><input value={contactName} onChange={e=>setContactName(e.target.value)} placeholder="Name" className="mt-4 w-full rounded-xl bg-white/5 p-3 text-sm text-white"/><div className="mt-3 flex gap-2"><input value={email} onChange={e=>{setEmail(e.target.value);setFound(null)}} placeholder="Email address" className="flex-1 rounded-xl bg-white/5 p-3 text-sm text-white"/><button onClick={searchPerson} className="rounded-xl bg-white/10 px-3">{lookupBusy?<Loader2 className="h-4 w-4 animate-spin"/>:<Search className="h-4 w-4"/>}</button></div>{found&&<div className="mt-3 rounded-xl bg-emerald-500/10 p-3 text-sm text-emerald-300">{nameOf(found.full_name,found.email)} is on Avelixa. Save and open chat.</div>}{!found&&email&&<div className="mt-3 rounded-xl bg-white/5 p-3 text-xs text-gray-400">If this email is not on Avelixa, Save & invite will send an invitation email and the recipient will create a password at the secure setup page.</div>}<button onClick={()=>void saveOrInvite()} className="mt-4 w-full rounded-xl bg-accent-600 py-3 text-sm font-semibold text-white">{found?'Save & open chat':'Save & invite'}</button></div></div>}{(notice||error)&&<div className={`fixed bottom-5 right-5 z-[90] max-w-sm rounded-xl border px-4 py-3 text-sm ${error?'border-red-500/20 bg-red-950 text-red-200':'border-emerald-500/20 bg-emerald-950 text-emerald-200'}`}>{error||notice}</div>}{incoming&&<Suspense fallback={null}><CallOverlay call={{id:incoming.id,callType:incoming.call_type,callerId:incoming.caller_id,calleeId:incoming.callee_id,remoteName:contacts.find(p=>p.user_id===incoming.caller_id)?.full_name||'Avelixa User',isIncoming:true,directConversationId:incoming.conversation_id}} onClose={()=>setIncoming(null)}/></Suspense>}{activeCall&&<Suspense fallback={null}><CallOverlay call={activeCall} onClose={()=>setActiveCall(null)}/></Suspense>}</div>;
 const clearChat=async()=>{if(!user||!selected)return;const now=new Date().toISOString();const {error:e}=await supabase.from('conversation_preferences').upsert({user_id:user.id,conversation_id:selected.id,cleared_at:now},{onConflict:'user_id,conversation_id'});if(e)setError(errOf(e));else{setMessages([]);setCalls([]);setNotice('Chat cleared.')}};
 const toggleMute=async()=>{if(!user||!selected)return;const next=!muted;const {error:e}=await supabase.from('conversation_preferences').upsert({user_id:user.id,conversation_id:selected.id,muted:next},{onConflict:'user_id,conversation_id'});if(e)setError(errOf(e));else setMuted(next)};
 const toggleBlock=async()=>{if(!selected||selected.kind!=='direct')return;const {error:e}=blocked?await supabase.rpc('communication_unblock_user',{p_blocked_id:selected.otherUserId}):await supabase.rpc('communication_block_user',{p_blocked_id:selected.otherUserId});if(e)setError(errOf(e));else setBlocked(!blocked)};
 const setWallpaperValue=async(value:string)=>{if(!selected||!user)return;setWallpaper(value);await supabase.from('conversation_preferences').upsert({user_id:user.id,conversation_id:selected.id,wallpaper:value},{onConflict:'user_id,conversation_id'})};
 const exportChat=()=>{if(!selected)return;const lines=timeline.map(x=>x.kind==='message'?`[${dateOf(x.at)}] ${x.item.sender_id===user?.id?'You':displaySelectedName}: ${x.item.content}`:`[${dateOf(x.at)}] ${x.item.call_type} call — ${x.item.status}`);const blob=new Blob([lines.join('\n')],{type:'text/plain'});const u=URL.createObjectURL(blob);const a=document.createElement('a');a.href=u;a.download='avelixa-chat.txt';a.click();URL.revokeObjectURL(u)};
}
